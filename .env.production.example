# 生产环境配置（复制为 .env.production 使用）
# docker compose -f docker-compose.prod.yml up -d --build

# DeepSeek API Key（必填）
DEEPSEEK_API_KEY=sk-your-deepseek-key

# AI Mock 模式（AI 不可用时 fallback，生产建议 false）
USE_AI_MOCK=false

# Web 端口（默认 80）
WEB_PORT=80
